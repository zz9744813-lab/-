# Dependencies
node_modules/

# Build output
.next/
out/
build/

# Type-check / build cache
*.tsbuildinfo

# Lockfiles (project uses pnpm; keep repo lockfile-agnostic for now)
package-lock.json
yarn.lock

# Local env / data
.env
.env.local
data/

# Editor
.DS_Store

# Runtime secrets (do not commit)
.env.production
.env.*.local

# Database files
data/*.db
data/*.db-*
data/backup-*

# Build artifacts
*.log
